/**
 * @deprecated Use `useCms()` — kept for backward compatibility during migration.
 */
export { useCms as useStrapi } from "~/composables/useCms";
export type { CmsListResponse as StrapiResponse } from "~/types/cms";
